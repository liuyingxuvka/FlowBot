## 1. Skill Installation

- [x] 1.1 Create the local `flowbot` Codex skill using the skill-creator workflow.
- [x] 1.2 Write a concise opt-in `SKILL.md` that points to this project and its startup/verification commands.
- [x] 1.3 Verify the installed skill is discoverable under the local Codex skills directory.

## 2. Version and Git Baseline

- [x] 2.1 Add a `VERSION` file with `0.1.0`.
- [x] 2.2 Add `.gitignore` for generated runs, temp files, caches, and local noise.
- [x] 2.3 Initialize local git and create the initial commit.
- [x] 2.4 Tag the initial commit as `v0.1.0`.

## 3. FlowGuard and Verification

- [x] 3.1 Add a small FlowGuard model for install/version/git ordering.
- [x] 3.2 Run OpenSpec validation, FlowGuard checks, startup intake smoke, and install verification.
- [x] 3.3 Update docs/adoption logs and record the initial version outcome.
